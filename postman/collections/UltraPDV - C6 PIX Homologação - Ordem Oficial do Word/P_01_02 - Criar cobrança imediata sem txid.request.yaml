$kind: http-request
url: "{{pix_base}}/cob"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "calendario": {
        "expiracao": 3600
      },
      "devedor": {
        "cnpj": "{{cnpj}}",
        "nome": "{{nome_devedor}}"
      },
      "valor": {
        "original": "1.00"
      },
      "chave": "{{chave_pix}}",
      "solicitacaoPagador": "Teste de homologacao UltraPDV sem txid"
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status esperado: 200', function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 3000
